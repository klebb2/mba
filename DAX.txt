Faturamento = CALCULATE( SUM(fVendas[Total]), ALL(dCalendario))
Faturamento Y-1 = 
CALCULATE(
  [Faturamento],
  DATEADD(dCalendario[Data], -1, YEAR)
)
Faturamento M-1 = 
CALCULATE(
  [Faturamento],
  DATEADD(dCalendario[Data], -1, MONTH)
)

Título = IF([Faturamento] > 0, "OK", "NOK")

Título 2 = IF([Faturamento] > 0, "YES", "NO")